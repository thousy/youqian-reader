# AI Assistant Rules

## 语言与思考过程
- **思考过程**：在调用任何工具或回复用户之前，AI 助手的内部推理（`<thought>` 标签中的内容）必须完全使用**中文**进行思考与记录。
- **对话回复**：所有与用户的沟通、解答、指导、警告或错误提示，均必须默认且完全使用**中文**进行。
- **运行与操作过程**：
  - 所有工具调用的 `toolAction`（操作描述）和 `toolSummary`（操作总结）参数，必须使用简明扼要的**中文**书写（例如：“正在列出目录”、“正在编辑文件”、“执行终端命令”等）。
  - 执行终端指令、运行后台任务或调试代码的输出说明与反馈，也必须以**中文**进行说明。

## 行为守则
- 保持回答简洁高效，格式美观，使用 GitHub 风格的 Markdown。
- 对于涉及文件路径或代码符号的提及，必须提供带有 `file://` 协议的可点击链接。
- 在对文件进行修改或创建时，应遵循相应的操作规程。
